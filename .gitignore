# 원본 PPT (대용량, 내부 자료 — 저장소에 포함 안 함)
*.pptx
*.ppt

# PPT 추출 임시 폴더 (build_photo_manifest.py로 재생성 가능)
ppt_extract/
ppt_extract_legacy/

# 슬라이드 텍스트 dump (재생성 가능)
legacy_slide_dump.txt

# 빌드 임시 스크립트 산출물
*.pyc
__pycache__/

# OS / 에디터 산출물
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~

# OneDrive 충돌 파일
*.~lock*
*conflict*

# IDE
.vscode/
.idea/
.claude/

# 환경 변수
.env
.env.local

# 노드(향후 사용 가능성)
node_modules/

# 임시 파일
*.tmp
*.bak

# 정리 보관함 (2026-05-21 루트 클린업) — git 추적 제외, OneDrive 로컬 보존
_archive/

# 월간회의 원본 엑셀 (대용량, 내부 자료 — 추출 결과인 schedule_data.js만 배포)
*.xlsx
*.xls

# 디버그/탐색용 임시 스크립트 (추적 제외)
_probe_*.py
_probe_*.out.txt
make_icons.py

# 사용자 노트
기대효과*.txt
